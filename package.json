{
    "name": "graduation-project-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run server --prefix backend\" \"npm run server --prefix langchain\" \"ollama serve\" \"npm run zap\"",
        "zap": "cd \"backend/services/Zed Attack Proxy\" && zap.bat -daemon -port 8080 -config api.key=123"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}