{
    "name": "graduation-project-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run server --prefix backend\" \"npm run dev --prefix langchain\""
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}